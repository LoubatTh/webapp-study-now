import { Search } from "lucide-react";

const SearchBar = () => {

  return (
    <div className="flex">
      <input type="search bar"></input>
      <button>
        <Search />
      </button>
    </div>
  );

}

export default SearchBar;
